# Milo skills

Add a role or capability later as a separate directory containing `SKILL.md`,
for example `skills/<name>/SKILL.md`. The Dockerfile already copies this tree to
OpenClaw's inherited skill path. Keep the core prompt role neutral and give
each new skill only the instructions and resources its capability needs.
