# Reference only. Use `plow-agents mint LINE_UID` for local credentials.
# Never commit a filled copy or a generated plow-credentials file.
PLOW_API_BASE=
PLOW_AGENT_TOKEN=

# Deliberately unset: a real AGENT_ID registers Milo on the Agent Index and
# starts usage reporting. Choose it only when ready to claim an index listing.
# AGENT_ID=
